# Server
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/vehicle_rental_db
spring.datasource.username=postgres
spring.datasource.password=Gourav15
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
jwt.secret=vehiclerentalsecretsignaturekey2024longerthan256bits
jwt.expiration=86400000

# Logging
logging.level.com.vehicle.rental=DEBUG
logging.level.org.springframework.security=DEBUG